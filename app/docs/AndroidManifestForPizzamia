<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="coffee.flavors.androidtutorials">

    <application
        android:name="coffee.flavors.androidtutorials.pizzamia.core.ApplicationPizzamia"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/PizzamiaAppMaterialTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="coffee.flavors.androidtutorials.pizzamia.views.ViewPizzaBox"
            android:theme="@style/PizzamiaAppMaterialTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
